[notifier]
name = notifier

pid_file = notifier.pid
foreground = no

listen_queue_len = 10
wait_on_address = localhost
wait_on_port = 5555

log_level = LOG_DEBUG

show_stats = yes
stats_file = notifier.stats
